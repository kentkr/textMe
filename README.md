textMe is a django webapp I created to explore backend development. 

# How it works

textMe uses pytesseract to detect the text of an image. It is extremely simple.

# Example

![Example image](example.png)
